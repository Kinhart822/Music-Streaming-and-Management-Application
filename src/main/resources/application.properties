spring.application.name=MSMA

# PostgreSQL DB connection
spring.datasource.url=jdbc:postgresql://switchyard.proxy.rlwy.net:58761/railway
spring.datasource.username=postgres
spring.datasource.password=ESnvSUVxddSydqffXNDKWUoQlXqlmsEy
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
security.jwt.secret-key=3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
security.jwt.access-expiration-time=600000
security.jwt.refresh-expiration-time=86400000

# MULTIPART (MultipartProperties)
    # Enable multipart uploads
spring.servlet.multipart.enabled=true
    # Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
    # Max file size.
spring.servlet.multipart.max-file-size=200MB
    # Max Request Size
spring.servlet.multipart.max-request-size=215MB
    # File Storage Properties
        # All files uploaded through the REST API will be stored in this directory
file.upload-dir=music_upload