spring.application.name=MSMA

# PostgreSQL DB connection
spring.datasource.url=jdbc:postgresql://localhost:5432/msma_database
spring.datasource.username=postgres
spring.datasource.password=kinhart822
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=huongpham71llp@gmail.com
spring.mail.password=lpgo dtdi racx uajv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# MULTIPART (MultipartProperties)
    # Enable multipart uploads
spring.servlet.multipart.enabled=true
    # Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
    # Max file size.
spring.servlet.multipart.max-file-size=200MB
    # Max Request Size
spring.servlet.multipart.max-request-size=215MB
    # File Storage Properties
        # All files uploaded through the REST API will be stored in this directory
file.upload-dir=music_upload

# Cloudinary
cloudinary.cloud-name=daw2gg78i
cloudinary.api-key=638655374275515
cloudinary.api-secret=

# Firebase
app.firebase-config=musicnotifications-9b666-firebase-adminsdk-fbsvc-153ab11431.json

